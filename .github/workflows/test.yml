name: Test

on:
  push:
    branches:
      - master
      - develop
  pull_request_target:
    branches:
      - master
      - develop

jobs:
  run-tests:
    if: github.event_name == 'push' || github.event_name == 'pull_request_target'
    permissions:
      contents: read
      checks: write
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v6
        with:
          ref: ${{ github.event.pull_request.head.sha }}
      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v6
        with:
          cache-read-only: false
      - name: Build and test
        run: ./gradlew reporter:shadowJar reporter:test
      - name: Sample multiplatform tests
        run: ./gradlew sample:allTests
        continue-on-error: true
      - name: Multiplatform Test Report
        uses: ./
        with:
          reports: "sample/build/test-results/**/*.xml"